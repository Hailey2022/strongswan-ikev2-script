vi /etc/crontab
# add this
# @reboot root ip link add ipsec0 type xfrm dev enp0s3 if_id 123 && ip link set ipsec0 up && ip route add 10.10.3.0/24 dev ipsec0 && ip route add fd12:3456:789a:1::/64 dev ipsec0
